[project]
name = "voicy-bot"
version = "0.1.0"
description = "Refactored Telegram bot with clean architecture"
dependencies = [
    "sqlalchemy",
    "alembic",
    "psycopg2-binary",
    "loguru"
]
requires-python = ">=3.12"
